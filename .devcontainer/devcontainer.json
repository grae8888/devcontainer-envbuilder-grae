{
    "build": {
        "dockerfile": "Dockerfile"
    },
    "onCreateCommand": "/install-docker.sh",
    "remoteUser": "coder",
    "runArgs": [
        "--privileged",
        "--network=host",
        "-h", "${localEnv:HOSTNAME}"
    ],
    "workspaceFolder": "/workspaces",
    "workspaceMount": "source=${localEnv:HOME}/envbuilder,target=/workspaces,type=bind",
    "containerEnv": {
        "DOCKER_TLS_CERTDIR": ""
    }
}
